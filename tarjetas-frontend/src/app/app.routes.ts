import { Routes } from '@angular/router';
import { TarjetasComponent } from './tarjetas/components/tarjetas.components';

export const routes: Routes = [
  { path: '', component: TarjetasComponent }
];
